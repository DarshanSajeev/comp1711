#include <stdio.h>

int main(){
    int temp = 12;
    if (temp <= 40 && temp >= -10)
    {
        printf("Temp is between -10 and 40\n");
    }
    else
    {
        printf("Temp is not between -10 and 40\n");
    }
}